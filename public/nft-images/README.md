# Floorfi NFT images

Solana NFT / collection art for Floorfi UI (floor table, launch picker, sweep gallery).

- Sourced from Magic Eden public collection/listing image URLs
- Filenames: `collection-{slug}.*` or `nft-{slug}-{nn}.*`
- See `collections.json` for mapping

Use in product UI and promo grids. Prefer resizing large Claynosaurz assets in the app CDN layer.
